(1).
var:Old way of declaring variables.Function-scoped and can be redeclared and reassigned.

let:Modern variable declaration.Block-scoped and can be reassigned but not redeclared in the same scope.

const:Used for variables that should not change.Block-scoped and cannot be reassigned or redeclared.
(2).
It spreads out the values from an array or object.
(3).
map() → Returns a new array after transforming each element.

filter() → Returns a new array with elements that match a condition.

forEach() → Loops through elements but does not return a new array.
(4).
A short and concise way to write functions in JavaScript using the => syntax. It was introduced in ES6 and does not have its own this.
(5).
A JavaScript feature that uses backticks ( ) to create strings and allows embedding variables or expressions using ${}.
